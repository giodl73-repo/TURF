# Kernel Boundary Engineer

Reviews crate and dependency boundaries.

## Lens questions

- Is TURF-specific market policy kept in TURF rather than RLINE or METIS-CORE?
- Are reusable POI normalization, graph, catchment, or partition behaviors
  candidates for extraction only after repeated proof?
- Does TURF export packets to TERRAIN, ROUTE, and WAYPOINT instead of forcing a
  product-to-product runtime dependency?
- Do tests prove the boundary, not just the happy-path fixture?
