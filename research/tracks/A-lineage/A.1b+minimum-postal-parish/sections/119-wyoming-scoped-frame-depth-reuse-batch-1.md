# Wyoming Scoped Frame Depth Reuse Batch 1

## Reusable Output

Scoped-frame depth source review:
`tables/wyoming-scoped-frame-depth-review-open-sources-2026-08-16.csv`.

Scoped-frame depth decisions:
`tables/wyoming-scoped-frame-depth-review-decision-2026-08-16.csv`.

Depth-review queue:
`tables/wyoming-scoped-frame-depth-review-queue-2026-08-16.csv`.

## Result

The first scoped-frame depth reuse batch ports the two prior selected-wave
Wyoming depth decisions into scoped-frame artifacts:

- Auburn,
- Carpenter WY.

The source-review artifact contains six rows: three Auburn evidence rows and
three Carpenter WY evidence rows. The decision artifact contains two rows.
Both rows retain the selected-wave source boundary and add scoped-frame
handling notes.

## Decisions

Auburn is not a scoped-frame numerator candidate in this batch. Star Valley
Sod geocodes inside the Auburn target box, so the row is carried as a
`postal_trade_service_minimum_candidate`; nearby Afton restaurants remain
outside-box context and are not inside-box contradictions.

Carpenter WY remains a scoped-frame depth survivor candidate. USPS confirms
the Carpenter post office inside the target box, while Antelope Truck Stop and
TA Cheyenne are real nearby fuel, food, and laundry sources that geocode
outside the Carpenter target box. This remains candidate status only until the
full scoped-frame depth and source-gated treatment close out.

## Remaining Work

After this reuse batch:

- 18 current-anchor-confirmed rows still need fresh scoped-frame depth review,
- 4 source-gated rows still need repair, contradiction, or explicit exclusion
  treatment before denominator or estimator use.

## Boundary

This batch does not create numerator rows, denominator rows, a scoped Wyoming
share, or any Mountain West, regional, or national frequency claim. It only
converts two reusable selected-wave depth decisions into scoped-frame decision
artifacts.
