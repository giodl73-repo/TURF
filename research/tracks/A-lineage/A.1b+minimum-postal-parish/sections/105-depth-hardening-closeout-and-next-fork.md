# Depth Hardening Closeout And Next Fork

## Reusable Output

Closeout fork table:
`tables/a1b-depth-hardening-closeout-and-next-fork-2026-08-16.csv`.

Probability/full-frame design contract:
`tables/a1b-probability-full-frame-design-contract-2026-08-16.csv`.

Supporting outputs:

- `tables/a1b-current-claim-packet-2026-08-16.csv`,
- `tables/a1b-survivor-evidence-ranking-2026-08-16.csv`,
- `tables/a1b-postal-only-candidate-truth-dossier-index-2026-08-16.csv`,
- `tables/a1b-truth-dossier-synthesis-2026-08-16.csv`,
- `tables/a1b-denominator-recomputed-summary-2026-08-16.csv`,
- `tables/a1b-denominator-exclusion-ledger-2026-08-16.csv`,
- `sections/106-probability-full-frame-design-contract.md`.

## Result

The depth-hardening phase is complete for the current active survivor set.

The survivor packet audit confirms:

- 9 active survivor packets,
- all 9 have source-review CSVs,
- all 9 have decision CSVs,
- all 9 decision statuses match the current truth synthesis.

The current selected-wave claim remains:

> In the completed selected breadth wave, 9 of 128 clean eligible target boxes
> are numerator-eligible postal-only candidates, with truth-dossier constraints
> attached.

The current candidate classes remain:

- seven strict target-box survivors with nearby consumer-service constraints,
- one strict target-box survivor with nearby trade-service context,
- one strict target-box survivor with outside-box consumer-service context.

## Next Fork

There are now two clean next paths.

If the next goal is discovery, add breadth:

- create a new versioned breadth manifest or extension manifest,
- keep source-gate and current-anchor controls,
- report any result as descriptive selected-wave evidence unless a probability
  design is added first.

If the next goal is regional, stratum, or national frequency language, do not
add purposive breadth as if it were probability evidence. Build the
probability/full-frame design first:

- define the frame,
- define inclusion probabilities or a census treatment,
- define weights or justify unweighted treatment,
- define source-gate and exclusion handling before estimating frequency.

The design contract in section 106 converts this fork into implementation
gates: frame scope, public-post-office semantics, sampling or census path,
strata, inclusion probabilities, estimator, source-gate policy, geocoder
policy, depth-review standard, and allowed reporting language.

Clayville remains a separate repair gate. It is not part of the current clean
denominator or survivor set unless an authoritative postal source resolves the
operations-status issue.

## Boundary

This closeout confirms the hardened selected-wave target-box packet. It does
not authorize complete service absence, catchment isolation, service adequacy,
trade areas, regional frequency, national frequency, income, spending, or
customer behavior.
