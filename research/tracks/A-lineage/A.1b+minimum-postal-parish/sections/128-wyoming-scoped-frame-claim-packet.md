# Wyoming Scoped Frame Claim Packet

## Reusable Output

Claim packet:
`tables/wyoming-scoped-frame-claim-packet-2026-08-16.csv`.

Supporting outputs:

- `tables/wyoming-scoped-frame-denominator-summary-2026-08-16.csv`,
- `tables/wyoming-scoped-frame-denominator-exclusion-ledger-2026-08-16.csv`,
- `tables/wyoming-scoped-frame-estimator-dry-run-2026-08-16.csv`,
- `tables/wyoming-scoped-frame-method-review-2026-08-16.csv`,
- `tables/wyoming-scoped-frame-numerator-roster-2026-08-16.csv`.

## Claim Packet

The current publishable unit is not a Mountain West or national prevalence
result. It is an internally reviewed scoped Wyoming target-box dry run.

Allowed current language:

> In this reviewed scoped Wyoming target-box dry run, 11 of 80 clean
> denominator rows are postal-only depth-survivor candidates.

Required adjacent context:

- the scoped frame has 84 Wyoming target boxes,
- four rows are excluded from the clean denominator,
- exclusions are one unresolved current anchor, two discontinued-current-anchor
  Meriden rows, and one outside-box Kaycee current-anchor row,
- the result is an exact scoped target-box dry-run count, not a probability
  estimate.

Allowed roster language:

The 11 scoped Wyoming depth-survivor labels are Carpenter, Fairview, Fort
Bridger, Grover, Hillsdale, Lance Creek, Osage, Pavillion, Recluse, Powder
River, and Smoot.

Blocked language:

- Mountain West regional frequency,
- national frequency,
- confidence intervals,
- probability estimate,
- catchment isolation,
- complete service absence,
- service adequacy,
- ZIP, city, county, trade-area, or customer-behavior coverage.

## Boundary

Use the numerator roster labels in prose. Do not expose raw generic seed labels
such as `Postal Service` or `U.S.` when current-anchor review resolves a
reporting-safe place label.

The next fork is either public wording review for the scoped Wyoming dry run or
replication of the full scoped-frame process in another state/frame.
