# Wyoming Scoped Frame Depth Batch 6

## Reusable Output

Updated scoped-frame depth source review:
`tables/wyoming-scoped-frame-depth-review-open-sources-2026-08-16.csv`.

Updated scoped-frame depth decisions:
`tables/wyoming-scoped-frame-depth-review-decision-2026-08-16.csv`.

## Result

The sixth scoped-frame depth batch closes the confirmed-inside depth queue by
adding five Wyoming depth decisions:

- Arvada,
- Recluse,
- Powder River,
- Smoot,
- Leiter.

After this batch, the scoped depth source artifact contains 37 rows and the
scoped decision artifact contains 20 rows. No current-anchor-confirmed depth
rows remain.

## Decisions

Arvada is reclassified out of postal-only status. Arvada Bar at 14 Railway St
geocodes inside the Arvada scoped target box, so it is treated as inside-box
food/drink service evidence.

Recluse remains a depth survivor candidate with nearby outside-service
context. The reviewed Gillette gas/convenience address geocodes outside the
Recluse scoped target box, and no current inside-box everyday-service
contradiction was found in this batch.

Powder River remains a depth survivor candidate after historical-service
checking. County 10 documents prior Powder River restaurant/cafe context, but
that evidence is treated as historical/defunct context rather than current
inside-box everyday-service evidence.

Smoot remains a depth survivor candidate with nearby outside-service context.
The reviewed Afton gas-service addresses geocode north of and outside the
Smoot scoped target box.

Leiter is reclassified out of postal-only status. Leiter Bar and Cafe at
4679 Hwy 14-16 E is corroborated by Travel Wyoming and the business website as
restaurant/bar service and geocodes inside the Leiter scoped target box.

## Remaining Work

After the reuse batch and batches 2 through 6:

- 0 current-anchor-confirmed rows remain for depth review,
- 4 source-gated rows still need repair, contradiction, or explicit exclusion
  treatment before denominator or estimator use.

## Boundary

This batch does not create numerator rows, denominator rows, a scoped Wyoming
share, or any Mountain West, regional, or national frequency claim. It only
completes the confirmed-inside scoped-frame depth-review queue. Source-gated
anchor treatment remains a blocking gate before denominator construction.
