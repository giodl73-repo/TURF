# Current Claim Packet And Survivor Ranking

## Reusable Output

Claim packet:
`tables/a1b-current-claim-packet-2026-08-16.csv`.

Survivor ranking:
`tables/a1b-survivor-evidence-ranking-2026-08-16.csv`.

Supporting outputs:

- `tables/a1b-denominator-recomputed-summary-2026-08-16.csv`,
- `tables/a1b-denominator-exclusion-ledger-2026-08-16.csv`,
- `tables/a1b-postal-only-candidate-truth-dossier-decisions-2026-08-16.csv`,
- `tables/a1b-truth-dossier-synthesis-2026-08-16.csv`,
- `tables/a1b-frequency-language-decision-gate-2026-08-16.csv`.

## Claim Packet

The current publishable unit is a bounded selected-wave packet, not a regional
or national prevalence result.

Allowed current language:

> In the completed selected breadth wave, 9 of 128 clean eligible target boxes
> are numerator-eligible postal-only candidates, with truth-dossier constraints
> attached.

Allowed candidate language:

- seven strict target-box survivors with nearby consumer-service constraints,
- one strict target-box survivor with nearby trade-service context,
- one strict target-box survivor with outside-box consumer-service context.

Blocked language:

- regional frequency,
- national frequency,
- confidence intervals,
- full-frame prevalence,
- complete service absence,
- catchment isolation,
- trade-area or customer-behavior claims.

## Survivor Ranking

The ranked survivor set is evidence-strength ordering for narrative use. It is
not a statistical ordering and it does not change numerator eligibility.

Alum Bridge WV remains the lead target-box example, but it is now constrained:
Somerville Exxon & Grocery in nearby Linn must be carried as outside-box
nearby-service context.

Artesian SD, Carpenter WY, Union Church MS, Clam Gulch AK, Aquilla TX, and
Mountain Home TX also remain strong target-box examples, but every narrative
use must carry the nearby-service constraint.

Amistad NM remains useful as a strict target-box example with nearby
trade-service context. Valley Park MS remains useful with the resolved
outside-box Vicksburg service context attached.

Clayville RI is no longer in the bounded numerator. Current USPS-indexed
evidence exposes a temporary-operations signal, so Clayville is a repair
candidate rather than a survivor until a later authoritative postal source
resolves the gate.

## Roadmap

Depth hardening is now complete for the current active survivor set.

The 9 active survivor packets now have source-review CSVs, decision CSVs, and
dossier prose. The packet audit reports nine packets with matching decision
statuses. Clayville remains a separate repair gate outside the clean denominator
and numerator.

Recommended next path depends on the goal:

- choose new descriptive breadth if the next goal is discovery and pattern
  recurrence,
- choose probability or full-frame design if the next goal is regional,
  stratum, or national frequency language.

A new breadth manifest can add pattern recurrence, but it will remain
descriptive selected evidence unless a probability or full-frame design is
built first.

Probability or full-frame work is the separate path if the next goal is
regional or national frequency language.
